[build]
  publish = "."
  command = "echo 'Sem build necessário — site estático + Functions'"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
